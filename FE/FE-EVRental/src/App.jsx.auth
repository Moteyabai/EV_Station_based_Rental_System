import React from 'react'
import './App.css'
import { AuthProvider } from './contexts/AuthContext'

function App() {
  return (
    <AuthProvider>
      <div style={{ padding: '20px', background: '#f0f0f0', minHeight: '100vh' }}>
        <h1 style={{ color: 'black' }}>Hello World with AuthProvider</h1>
        <p style={{ color: 'black' }}>Testing React app with AuthProvider</p>
      </div>
    </AuthProvider>
  )
}

export default App